site/ mirrors what's deployed to oaica-install.pages.dev / oaica.com (custom domain pending). install.sh and install.ps1 there are copies of scripts/{install.sh,install.ps1} — redeploy after any script change: wrangler pages deploy site --project-name oaica-install --branch main
